/// <reference path="./node_modules/@types/p5/global.d.ts" />

/*
General Idea: Have a generative art that is seeded by the current minute
*/

function setup() {
    createCanvas(windowWidth - 20, windowHeight - 20);
}

function draw() {
    background(220);
}
